# results

Local output directory for benchmark runs, experiment artifacts, and generated reports.

## What Lives Here
- benchmark result bundles such as `deic_cogbench/`
- mechanism-validation outputs such as `dynamic_structure_learning/`
- frontier/local model results such as `frontier/` and `frontier_local/`
- older preserved outputs under `legacy/`

## Notes
- Most contents here are generated artifacts rather than hand-maintained source.
- Keep reproducible outputs here and avoid mixing them with core code.

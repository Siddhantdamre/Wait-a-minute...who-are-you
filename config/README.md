# config

Small configuration helpers and mode switches for running the project in different environments.

## What Lives Here
- `run_mode.py`: runtime mode selection helpers.

## Notes
- Keep configuration changes narrow and explicit here.

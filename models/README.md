# models

Thin wrappers for language-model and embedding backends.

## What Lives Here
- `llm.py`: language-model interface helpers.
- `embeddings.py`: embedding utilities.

## Notes
- These are infrastructure helpers rather than benchmark definitions.
